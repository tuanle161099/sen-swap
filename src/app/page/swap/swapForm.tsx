import { Card } from 'antd'
import Swap from 'app/components/swap'

const SwapForm = () => {
  return (
    <Card bordered={false}>
      <Swap onChange={() => {}} />
    </Card>
  )
}

export default SwapForm
